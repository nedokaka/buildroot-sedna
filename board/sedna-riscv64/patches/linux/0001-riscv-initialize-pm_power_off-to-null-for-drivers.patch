null-for-drivers.patch
--- linux-6.4.12/arch/riscv/kernel/reset.c
+++ linux-6.4.12/arch/riscv/kernel/reset.c
@@ -12,7 +12,7 @@
                wait_for_interrupt();
 }

-void (*pm_power_off)(void) = NULL;
+void (*pm_power_off)(void);
 EXPORT_SYMBOL(pm_power_off);

 void machine_restart(char *cmd)
@@ -23,12 +23,16 @@

 void machine_halt(void)
 {
-       do_kernel_power_off();
-       default_power_off();
+        if (do_kernel_power_off)
+               do_kernel_power_off();
+        else
+              default_power_off();
 }

 void machine_power_off(void)
 {
-       do_kernel_power_off();
-       default_power_off();
+        if (do_kernel_power_off)
+               do_kernel_power_off();
+        else
+               default_power_off();
 }
